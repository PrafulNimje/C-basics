// 4. multiplication of two integers
int main(){
int num1, num2, mul;
    

    printf("Enter two numbers for division: ");

    scanf("%d %d", &num1, &num2);
    mul = num1* num2;

    printf("Division : %d",mul);

    return 0;
}